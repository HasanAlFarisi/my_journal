<div class="post_body">
    <h2><a href="#">Our Team</h2>
  
  <div class="post_content_admin">
        <% admins.each do |admin| %>
              <%= link_to image_tag(admin.avatar.url(:original), style:"width:90px;height:90px;", class:"floating"), dashboard_profile_show_admin_path(admin), remote: true %>
              
              <!-- <article class='profile_desc'>
                  <b><%= "#{admin.name} #{admin.last_name}" %></b>
             </article> -->
                    <!-- <b><%#= "#{admin.name} #{admin.last_name}" %></b>
                    <br>
                    <% admin.profile_skills.limit(3).each do |skill| %>
                          <%#= skill.name %><br>
                    <% end %>
                    

              </article>    -->
        <% end %>
  </div>
</div>

<div id="show_modal_admin_profile"><!-- Place for modal admin profile --></div>
<div id="modal_background"></div>