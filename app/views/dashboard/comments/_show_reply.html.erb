<tr class="hidden_<%="#{comment.id}"%>">	
	<td><%= comment.body %></td>
	<td  width="5px"><%= link_to 'Destroy', dashboard_comment_destroy_reply_path(comment), method: :delete, remote: true, :class => "ico del", onclick:"hidden_delete(#{comment.id})" %>
	</td>
</tr>

