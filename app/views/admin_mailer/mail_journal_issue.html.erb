<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <%= stylesheet_link_tag "style-admin", :media => "all" %>
    <%= stylesheet_link_tag "mailer_notification", :media => "all" %>
  </head>
  <body class="journal_issue">
    <%#= generated_status(@journal_issue.status_id).html_safe %>
    <h1><%= find_type(@journal_issue.type_id).html_safe %> <%= link_to "##{@journal_issue.no}" " #{@journal_issue.title}",  admin_journal_issue_path(id: @journal_issue.id,id_journal: @journal.id) %></h1>
    <p>Hi <%= "#{@admin.name} #{@admin.last_name}" %>, you have one Journal 
    <% if @type == "plus" %>
        As Project Quality Check
    <% end %>
    </p>
    <p>
      Below is Journal Details:
      <table border="0">
        <tr>
              <th>Journal Project |</th>
              <th>Journal Assign |</th>
              <th>Journal Progress |</th>
              <th>Journal Priority</th>
        </tr>
        <tr>
              <td>
                      <%= @journal.title %>
              </td>
              <td>
                      <%= "#{find_profile(@journal_issue.asignee).name} #{find_profile(@journal_issue.asignee).last_name}" %>
              </td>
              <td>
                      <%= @journal_issue.progress.blank? ? 0 : @journal_issue.progress %>%
              </td>
              <td>
                      <%= find_priority(@journal_issue.priority) %>
              </td>
        </tr>
        <tr>
              <td colspan="4">
                    Start <b><%= @journal_issue.start.strftime('%d-%b-%Y') %></b> End <b><%= @journal_issue.end.strftime('%d-%b-%Y') %></b>
              </td>
        </tr>
        <tr>
              <td>
                    <%= raw @journal_issue.content %>
              </td>
        </tr>
        <tr>
              <td colspan="4">
                  For more Detail about this Journal, you can click this 
                  <a href="https://journalkreativ.herokuapp.com/admin/journal_issues/<%= @journal_issue.id %>?id_journal=<%= @journal.id %>">Link</a>
              </td>
        </tr>
      </table>
    <p>Thank you,
    <%= @company %> System</p>
  </body>
</html>