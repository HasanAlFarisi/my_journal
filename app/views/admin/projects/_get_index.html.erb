<div class="panel panel-primary">
      <div class="panel-heading">
          <i class="fa fa-bar-chart-o fa-fw"></i><spans class="assign">Project(s)</spans>
      </div>

      <div class="panel-body">
          <div class="row">
              <div class="col-lg-12">
                  <div class="table-responsive">
                      <table class="table table-bordered table-hover table-striped">
                          <thead>
                              <tr>
                                 <th width="13">No</th>
				       <th>Title</th>
				       <th>Status</th>
				       <th>Assign</th>
				       <th><center>Journal</center></th>
				       <th><center>Estimated Time</center></th>
				       <th colspan="2"><center>Action</center></th>
                              </tr>
                          </thead>
                          <tbody class="get_index">
                                 <tr class="ajaxs hide">
                                       <td colspan="8"><center><%= render 'dashboard/shared/ajax_loader' %></center></td>
                                 </tr>
                                 <%= render "admin/projects/get_index_data" %>
                          </tbody>
                      </table>
                      <span class="paggings">
                          <%= render "admin/projects/shared/paggings" %>
                      </span>
                  </div>

              </div>

          </div>
          <!-- /.row -->
      </div>
      <!-- /.panel-body -->
  </div>
<%# @admin_journals.each do |project| %>
<%# end %>