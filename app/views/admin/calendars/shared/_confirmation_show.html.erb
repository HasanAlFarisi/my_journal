<%= "#{find_profile(@member.admin_id).name} #{find_profile(@member.admin_id).last_name}" %> <%= @member.status == "not_sure" || @member.status == nil ? "<u class='confirm'>Need Confirm</u>".html_safe : @member.status == "true" ? "<u class='join'>Join</u>".html_safe : "<u class='n_join'>Not Join</u>".html_safe %>