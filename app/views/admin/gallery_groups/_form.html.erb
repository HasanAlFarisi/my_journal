<%= form_for(@admin_gallery_group) do |f| %>
  <% if @admin_gallery_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_gallery_group.errors.count, "error") %> prohibited this admin_gallery_group from being saved:</h2>

      <ul>
      <% @admin_gallery_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sub_name %><br>
    <%= f.text_field :sub_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
